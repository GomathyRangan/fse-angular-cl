import { UserNameFilterPipePipe } from './user-name-filter-pipe.pipe';

describe('UserNameFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserNameFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
