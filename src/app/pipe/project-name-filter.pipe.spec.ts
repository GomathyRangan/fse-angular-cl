import { ProjectNameFilterPipe } from './project-name-filter.pipe';

describe('ProjectNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
