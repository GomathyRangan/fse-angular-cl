import { TaskNameFilterPipe } from './task-name-filter.pipe';

describe('TaskNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
